# خطة نقل بيانات g000st إلى PostgreSQL

**الحالة:** خطة تنفيذ، لم يبدأ الترحيل. **التاريخ:** 25 سبتمبر 2026.

## 1. القرار والهدف

- تصبح PostgreSQL مصدر الحقيقة الوحيد لبيانات المنتج: الحسابات والجلسات، الملفات الشخصية، الأصدقاء، Social، Market، Chat، الحضور، الإشعارات المسجلة، المكالمات، SMS، والأرصدة وسجلها.
- يبقى `apps/api` الواجهة الوحيدة للبيانات أمام الهاتف والويب. تبقى عقود `/api/v1` وسلوكها كما هي؛ أي تغيير ظاهر يمر بإصدار عقد صريح.
- يبقى Firebase Cloud Messaging لإشعارات مكالمات Android الضرورية. تبقى APNs لمكالمات iOS، وخدمة Expo Push لإشعارات التطبيق العادية. PostgreSQL تخزن تسجيلات الأجهزة وحالة الإرسال، لكنها لا ترسل Push بنفسها. يمكن تشغيل FCM من خادم API مستقل عن Firestore. [توثيق FCM](https://firebase.google.com/docs/cloud-messaging/fcm-architecture)
- تبقى ملفات الصور والصوت والفيديو في التخزين المتوافق مع S3/MinIO. PostgreSQL تخزن مفاتيح الكائنات والبيانات الوصفية فقط؛ تبقى Stripe وTelnyx وTURN خارج قاعدة البيانات بوصفها خدمات مستقلة.
- لا نعيد بيانات العميل القديمة من فرع الأرشيف إلى `main`. يشمل النقل بيانات `apps/api` الفعلية في بيئاتها المعتمدة فقط.

هذه الخطة تحل محل قرار التأجيل السابق في [خطة البنية](INFRASTRUCTURE_PLAN_AR.md)، بعد موافقة صاحب المشروع على الانتقال. لا تعني الموافقة بدء تحويل بيانات الإنتاج أو حذف Firestore؛ ذلك يحدث فقط عند بوابات القطع أدناه.

## 2. صورة الوضع الحالي

| المجال | المصدر الحالي في API | ملاحظات النقل |
| --- | --- | --- |
| Auth | `FirestoreAuthStore` | حسابات، بصمة Recovery ID، جلسات access/refresh وعائلاتها؛ لا يوجد Recovery ID صريح في الخادم. |
| Social وMarket | `FirestoreSocialStore`, `FirestoreMarketStore` | منشورات، وسائط، تعليقات، تفاعلات، ملفات شخصية، متابعة، تنبيهات، بلاغات؛ بعض المجموعات الفرعية داخل المستندات. |
| Chat وContacts وPresence | `FirestoreChatStore`, `FirestoreContactsStore`, `FirestorePresenceStore` | محادثات ثنائية، تلخيص لكل عضو، رسائل مؤقتة وBurn، أسماء أصدقاء، آخر نشاط. |
| Notifications وCalling | `FirestoreNotificationStore`, `FirestoreCallingStore` | أجهزة Expo Push، رموز VoIP، سجل المكالمات؛ FCM يعتمد اليوم على تهيئة Firebase Admin التي ينشئها Firestore. |
| Billing وTelephony | `FirestoreBillingStore`, `FirestoreTelephonyStore` | أرصدة وحدات، دفتر قيود idempotent، مكالمات/SMS Telnyx وwebhooks. |

تجميع الخدمات الحالي موجود في [`apps/api/src/server.ts`](../apps/api/src/server.ts). أغلب المجالات تملك `*Store` interface بالفعل، وهذا هو حد الاستبدال المقصود. لا نربط العملاء بقاعدة البيانات ولا نعيد بناء API من الصفر. تستعمل الواجهة الجديدة أنواع TypeScript وعقود `v1` الحالية.

## 3. البنية المستهدفة

```text
Mobile / Web  --> HTTPS --> apps/api --> PostgreSQL (شبكة خاصة)
                               |-----> MinIO / S3 (الملفات)
                               |-----> Expo Push / FCM / APNs (الإشعارات)
                               |-----> Stripe / Telnyx / TURN (الخدمات الخارجية)
```

1. **الاستضافة:** قاعدة PostgreSQL مستقلة عن عمليتي Next.js وAPI وعن خادم MinIO، على خادم قاعدة بيانات خاص أو خدمة PostgreSQL مُدارة داخل شبكة خاصة. لا يكفي حجزها على خادم `g000st-web` الحالي ذي 3.7 GB RAM وقرص 38 GB دون قياس وميزانية موارد. القرار الافتراضي هو خادم قاعدة بيانات خاص داخل شبكة خاصة، مع إمكان اختيار خدمة مُدارة إذا كانت الإدارة التشغيلية أقل كلفة. لا نختار حجم الخادم قبل قياس البيانات ومعدل العمليات.
2. **الفصل:** قاعدتان منفصلتان لـstaging وproduction، بمستخدم تطبيق محدود الصلاحيات لكل بيئة ومستخدم migrations منفصل. لا تستخدم `AUTH_COLLECTION_PREFIX` لتقليد فصل البيئات في SQL. لا منفذ PostgreSQL عام؛ السماح لاتصال API فقط، مع TLS وتوثيق SCRAM وصلاحيات محدودة. [توثيق مصادقة PostgreSQL](https://www.postgresql.org/docs/current/auth-password.html)
3. **الربط:** pool واحد مضبوط لكل عملية API، بحد اتصالات مشتق من عدد عمليات PM2 وحد `max_connections`. timeouts للاستعلامات والمعاملات وhealth check حقيقي لقاعدة البيانات. migrations محفوظة في `apps/api` وذات رقم إصدار؛ تُشغّل كخطوة نشر مستقلة قبل تحويل الكود، ولا تُشغّل تلقائيًا من كل نسخة API.
4. **Firebase المتبقي:** نفصل تهيئة Firebase Admin اللازمة لـFCM عن `createFirestore()`. عندما تختفي آخر Store من Firestore، لا نُبقي `getFirestore()` في بدء التشغيل. تُحصر بيانات اعتماد FCM في الخادم؛ يظل تسجيل أجهزة Expo/VoIP في PostgreSQL.
5. **الزمن والملفات:** تخزين الوقت في `timestamptz` UTC، والتحويل من/إلى `*AtMs` عند حد Store حتى يبقى عقد API كما هو. لا تخزين base64 أو ملفات ثنائية في SQL.

## 4. نموذج البيانات الأولي

هذه خريطة تصميم وليست SQL نهائيًا. تُعتمد DDL والقيود بعد جرد الحقول الفعلية والعينات في المرحلة 0. نستخدم مفاتيح Public ID الحالية ومعرّفات المستندات القائمة؛ لا نولّد هوية جديدة أثناء الترحيل.

| مجموعة الجداول | الجداول المقترحة | قيود وفهارس مهمة |
| --- | --- | --- |
| الهوية | `accounts`, `recovery_credentials`, `session_families`, `access_sessions`, `refresh_sessions` | Public ID فريد؛ lookup hash فريد؛ hash فقط للأسرار والرموز؛ فهارس انتهاء الصلاحية وfamily؛ حفظ replay detection لدوران refresh. |
| الملف والعلاقات | `profiles`, `contacts`, `follows`, `presence` | `(owner_id, contact_id)` فريد؛ الاسم البديل خاص بصاحب القائمة؛ علاقات المتابعة فريدة؛ حذف الحساب يحوّل العرض إلى `Deleted account` مع حفظ المراجع المطلوبة. |
| Social | `social_posts`, `social_post_media`, `social_comments`, `social_reactions`, `social_alerts`, `social_reports` | حفظ `visibility` للمنشورات/التعليقات؛ حفظ `shared_post_id`؛ قيود مالك وتفاعل فريد؛ فهرس `(created_at DESC, id DESC)` للـcursor. لا يُستنتج ظهور هوية الكاتب من بياناته المخزنة عند عرض منشور مجهول. |
| Market | `market_posts`, `market_post_media`, `market_comments`, `market_reactions` | السعر بقيمة عشرية محددة الدقة والعملة من العقد، مع `allow_calls` و`allow_video_calls`؛ فهارس feed والمالك؛ media object key فقط. |
| Chat | `conversations`, `conversation_members`, `chat_messages`, `chat_attachments` | طرفان فقط لكل محادثة، مفتاح زوج مرتب وفريد؛ `(conversation_id, sender_id, client_message_id)` فريد لمنع التكرار؛ فهرس `(conversation_id, created_at DESC, id DESC)`؛ فهرس `expires_at` للحذف؛ حالة read/Burn وملخص كل عضو. |
| الإشعارات والمكالمات | `push_devices`, `voip_devices`, `calls`, `outbox_events` | `(public_id, device_id)` فريد؛ رموز الأجهزة خاصة؛ `outbox_events` يحفظ العمل الخارجي القابل لإعادة المحاولة بعد commit. |
| الفوترة والهاتف | `billing_balances`, `billing_ledger`, `telephony_calls`, `telephony_sms`, `webhook_events` | مفتاح idempotency فريد لكل أثر مالي، تحديث الرصيد والدفتر في معاملة واحدة؛ معرفات Stripe/Telnyx/webhook فريدة؛ ترتيب سجلات cursor مركب. |

**قواعد لا تُفقد أثناء النقل:** لا تخزين Recovery ID الصريح؛ الحساب المحذوف لا يستطيع الدخول أو الإرسال، وتبقى الآثار التي يتطلبها المنتج باسم `Deleted account`. لا تتغير مدة الرسائل العادية (ساعتان) ولا Burn (خمس ثوانٍ بعد الفتح). لا تُنسخ الرسائل المنتهية أو التي حُذفت فعليًا. لا تُفقد مفاتيح الوسائط أو العلاقة بين الرسالة/المنشور والملف. تُراجع سياسة الاحتفاظ بنص SMS والبلاغات وسجلات webhooks قبل تثبيت DDL.

## 5. قواعد التنفيذ في الكود

- ننشئ `Postgres*Store` مطابقًا لكل interface موجود، مع اختبارات عقد مشتركة تشغّل السلوك نفسه على بديل Firestore وPostgreSQL حيث أمكن. لا نضع SQL في `apps/mobile` أو `apps/web`.
- نعزل المنطق المشترك، مثل بناء cursors والتحقق من العضوية، في الخدمات الحالية؛ تتولى Stores القراءة والمعاملات. تُستبدل وثائق Firestore الفرعية بصفوف وجداول علاقات، ولا ننقل شكل المستندات حرفيًا إلى عمود JSONB عام.
- لكل عملية تعدّل عدة جداول، معاملة قصيرة وواضحة. قفل الصف المناسب في الرصيد، read state، وتدوير refresh؛ فهارس uniqueness تمنع التكرار عند السباق. لا يُجرى نداء Stripe/Telnyx/FCM داخل معاملة SQL.
- نستخدم outbox للآثار التي لا يمكن ضمها ذريًا إلى SQL (Push، حذف ملف MinIO، وبعض webhooks) مع worker وإعادة محاولة ومفتاح idempotency. ذلك مهم خاصة بعد حذف رسائل Chat وانتهاء صلاحيتها.
- نحافظ على `clientMessageId` وcursors والقيود الأمنية الموجودة. يكون استعلام كل feed بترتيب ثابت، ويُقارن الناتج مع Firestore قبل أي قطع.
- تبقى بيانات اعتماد PostgreSQL وFirebase وStripe/Telnyx في بيئة الخادم فقط، خارج المستودع. لا تُكتب Recovery IDs أو tokens أو نصوص الرسائل في logs أو أدوات المقارنة.

## 6. طريقة ترحيل البيانات

### 6.1 الجرد والتحضير

1. جرد كل collections وsubcollections والعدادات والحقول الفعلية لكل بيئة، مع عدد المستندات وحجمها ومعدل التغيير اليومي؛ ربطها بالجدول المستهدف وتوثيق الحالات غير المعتادة. مراجعة حقول أضيفت في العمل الجاري قبل تثبيت المخطط.
2. تصنيف البيانات: مصدر حقيقة، بيانات مشتقة يمكن إعادة حسابها، بيانات منتهية، وأسرار. تحدد عينة staging مجهولة الهوية أخطاء الأنواع والحقول المفقودة من دون نشر أسرار.
3. نسخة احتياطية لـFirestore قبل كل تجربة قطع. تصدير Firestore المُدار صالح للاحتفاظ والاستعادة إلى Firestore، لكنه **ليس ملف SQL جاهزًا** ولا لقطة متطابقة تمامًا عند بداية التصدير؛ لذلك نبني قارئ ترحيل محكومًا من Admin SDK أو مرحلة تحويل صريحة، مع watermark/نافذة كتابة مجمدة. [توثيق Firestore للتصدير](https://firebase.google.com/docs/firestore/manage-data/export-import)
4. قياس سعة PostgreSQL المتوقعة: حجم الصفوف والفهارس والـWAL والنمو والنسخ الاحتياطي، ثم اختيار حجم الخادم وموقعه وميزانية شهرية. لا نفترض أن PostgreSQL أو Firestore أوفر قبل قياس القراءة والكتابة والتشغيل.

### 6.2 النقل التجريبي والقابل للإعادة

1. migrations SQL بإصدارات ثابتة، ثم برنامج `apps/api/scripts` مكتوب TypeScript للقراءة على دفعات وكتابة idempotent إلى staging. يحتفظ بمعرّفات المصدر ويرصد تقدم كل مجموعة في جدول `migration_runs` أو ملف حالة آمن خارج Git.
2. ترتيب الأب/الابن: accounts ثم credentials/sessions/profiles، ثم العلاقات والمنشورات والوسائط، ثم المحادثات والأعضاء والرسائل، ثم أجهزة Push والمكالمات، ثم الأرصدة والدفتر والهاتف. تؤجل FKs التي تعيق الاستيراد إلى ما بعد التحقق مع معالجة tombstones.
3. تحويل Firestore Timestamp وأرقام الميلي ثانية إلى UTC؛ حفظ المال/السعر بدقة مناسبة؛ التعامل مع مستندات ناقصة، ردود الفعل المكررة، المراجع اليتيمة، حالات الحذف، والمجموعات الفرعية. لا نعيد حساب أرصدة مالية من تخمين؛ نقارن الرصيد مع مجموع ledger ونحقق الفروق يدويًا.
   ننقل قيم `salt` و`verifier` وlookup hash وعائلات الجلسات والرموز المجزأة كما هي. يبقى `AUTH_RECOVERY_PEPPER` نفسه أثناء النقل؛ تغييره من دون إجراء تدوير مصمم سيمنع استعادة الحسابات. بعد القطع نتحقق من دخول حساب قديم وتدوير refresh token قديم ومنع إعادة استخدامه.
4. جرد عدد الصفوف لكل مجموعة ومفاتيح المستندات، ومقارنة مجموعات عينات عشوائية وآخر/أول صفحة لكل cursor، وإعادة حساب العدادات المشتقة ومقارنتها. تقارير المقارنة تخزن IDs أو hashes فقط ولا تحمل أسرارًا.
5. تشغيل migration dry run كامل على نسخة staging ومعه اختبار استعادة نسخة PostgreSQL. ثم تشغيل تطبيق staging فعليًا فوق PostgreSQL مع اختبار مستخدمين على الهاتف والويب.

### 6.3 التحويل الفعلي

**الخيار الافتراضي عند حجم بيانات معتدل:** نبني ونختبر Stores مجالًا مجالًا في التطوير وstaging، لكن **نحوّل مصدر الحقيقة للإنتاج كله في نافذة صيانة واحدة**. السبب أن إنشاء/حذف الحساب، الملف، الرسائل والإشعارات والرصيد يتقاطع بين المجالات؛ قطع بعضها للإنتاج وحده يخلق حالة مزدوجة يصعب ضمانها. أثناء النافذة نوقف كتابات API والعامل والمهام الدورية، وننسق استقبال webhooks الخارجية أو حفظها في طابور دائم لإعادة المعالجة. نلتقط نقطة قطع، ننقل الفرق الأخير، نراجع invariants، ثم نحول جميع Stores إلى PostgreSQL ونفتح الكتابات بعد فحص الصحة. لا نستخدم dual-write مرتجلًا لأن فشل نصف العملية يخلق مصدرين مختلفين. إذا أظهرت القياسات أن نافذة الصيانة غير مقبولة، تُصمم مرحلة CDC/outbox مزدوجة الاتجاه مع مصالحة صريحة قبل التنفيذ؛ ليست افتراضًا في هذه الخطة.

**ترتيب البناء والاختبار في staging:** Presence/Contacts، ثم Social/Market، ثم Notifications/Calling، ثم Chat، ثم Auth، وأخيرًا Billing/Telephony. قبل تجربة cutover كاملة نعيد نسخ Accounts وكل المراجع من لقطة واحدة، ونختبر إنشاء/حذف الحساب أثناء عمل بقية الخدمات. لا نطبّق FK عابرًا لمصدرين أثناء التجارب الجزئية. Billing/Telephony ينتقلان كوحدة واحدة حتى لا يتباعد الرصيد عن Webhooks.

**الرجوع:** قبل فتح الكتابات على PostgreSQL، يمكن الرجوع إلى Firestore بعد التحقق من أن النسخة الأصلية لم تتغير. بعد فتح الكتابات، تبديل العلم وحده إلى Firestore غير آمن؛ يتطلب إيقاف الكتابة، نقل العمليات الجديدة عكسيًا والتحقق منها، وإلا نصلح PostgreSQL للأمام. يحتفظ الفريق بنسخة Firestore للقراءة فقط فترة مراقبة محددة بعد القطع، ثم تؤرشف وتحذف ضمن سياسة الاحتفاظ. لا يُحذف أي مصدر قبل نجاح النسخ الاحتياطي والاستعادة وموافقة تشغيلية صريحة.

## 7. ترتيب المراحل وبوابات القبول

| المرحلة | المخرجات | شرط الانتقال |
| --- | --- | --- |
| 0. الجرد والقرارات | إحصاءات فعلية، خريطة collections، تقدير سعة وكلفة، RPO/RTO ونافذة الصيانة، مخطط DDL أولي | لا مجموعة مجهولة أو حقل مالي غير مفهوم؛ اعتماد مكان استضافة PostgreSQL وخطة النسخ. |
| 1. البنية | خادم/خدمة PostgreSQL، staging/prod معزولان، اتصال خاص، roles، monitoring، نسخ أساسية وWAL، تجربة restore | استعادة عملية على خادم منفصل، اختبار انقطاع واتصال، تنبيهات disk/WAL/backup. |
| 2. الأساس في API | driver/pool، migrations، health، script ترحيل واختبارات عقد Stores | API staging يقلع من PostgreSQL، migrations قابلة للإعادة، لا أسرار في logs. |
| 3. المجالات الأقل خطورة | Presence، Contacts، ثم Social/Market وNotifications/Calling في التطوير وstaging | تطابق القوائم والعدادات والصلاحيات، نسخ الوسائط المرجعية، QA ويب وهاتف بحسابين. |
| 4. المجالات الحرجة | Chat ثم Auth ثم Billing/Telephony في staging وتجربة قطع كاملة | اختبارات السباق والحذف وBurn، استمرار كل الحسابات والجلسات، عدم تكرار قيد مالي أو webhook، توافق Push والمكالمات. |
| 5. القطع والمراقبة | تقرير مقارنة نهائي، تحويل production المنسق في نافذة واحدة، مراقبة وrunbook | نجاح بوابات القسم 8، عدم وجود فرق بيانات أو خطأ مالي، خطة رجوع قابلة للتنفيذ. |
| 6. التنظيف | إزالة Firebase Admin Firestore فقط، حصر FCM، تحديث docs/CI/deploy، أرشفة Firestore | لا استدعاءات Firestore في API المنتج أو أدوات النشر، والإشعارات تعمل؛ الاحتفاظ بالنسخ وفق السياسة. |

**تقدير مبدئي للتخطيط:** عدة مراحل تطوير واختبار ونقل، غالبًا 6–10 أسابيع لمهندس متفرغ واحد إذا كانت البيانات قليلة وخدمات staging مستقرة؛ يُعاد التقدير بعد المرحلة 0، ولا يُستعمل الرقم موعدًا للنشر.

## 8. الاختبارات ومؤشرات النجاح

- اختبارات API الحالية كلها، ثم اختبارات تكامل PostgreSQL حقيقية لعمليات auth refresh المتزامنة، الحذف، جلب feed بالـcursor، تكرار `clientMessageId`، read/unread، Burn، انتهاء صلاحية الرسائل والوسائط، race على follow/like، وتكرار Stripe/Telnyx webhooks.
- فحوص end-to-end على staging: حساب جديد واستعادة حساب قديم، تسجيل دخول من جهازين، Social/Market وملفات الصور، Friends وChat، إشعار Expo عادي، مكالمة Android عبر FCM وiOS عبر APNs، شراء/رصيد وSMS تجريبي. تبقى اختبارات الأجهزة الحقيقية لازمة للخدمات التي لا تكفي محاكاتها.
- جرد قبل/بعد لكل جدول من حيث العدد والمفاتيح، مقارنة عينات كاملة بعد حذف الحقول الحساسة، تحقق المراجع والعدادات، ledger balance، عدد الأجهزة النشطة، الرسائل غير المنتهية، وتطابق أول وآخر صفحات feed.
- مراقبة p50/p95/p99 للاستعلامات وAPI، أخطاء SQL وpool، deadlocks، معدل نمو WAL/القرص، replication/backup lag، عمر آخر نسخة ناجحة، backlog للـoutbox وchat expiration. تحدد حدود إنذار رقمية من baseline المرحلة 0، لا من أرقام تخمينية.
- بوابة الإنتاج: صفر فروق غير مفسرة في الحسابات والرصيد والدفتر؛ صفر فقد لرسائل غير منتهية في العينة الكاملة؛ جميع اختبارات العقد والـE2E ناجحة؛ نسخ احتياطي حديث واستعادة مُجرّبة؛ نافذة قطع موثقة؛ المسؤول التشغيلي حاضر.

## 9. التشغيل والأمن والنسخ الاحتياطي

- نسخة أساسية دورية + أرشفة WAL خارج خادم قاعدة البيانات ومشفرة، مع اختبار استعادة شهري على بيئة منعزلة. هدف أولي قابل للتعديل بعد المرحلة 0: **RPO ≤ 15 دقيقة، RTO ≤ ساعتين**. تُراقب فجوات أرشفة WAL، ولا يُعد `pg_dump` وحده بديلًا عن PITR. [توثيق PostgreSQL لـPITR](https://www.postgresql.org/docs/current/continuous-archiving.html)
- منفذ قاعدة البيانات خاص، TLS، SCRAM، حسابات منفصلة للتطبيق والترحيل والنسخ والقراءة التشغيلية، تدوير الأسرار، firewall، وتشفير أقراص/نسخ احتياطية. لا يستخدم API حساب superuser. تُراجع استعلامات بطيئة وامتيازات الوصول بانتظام.
- تحديد retention للرسائل المنتهية، بيانات الأجهزة، SMS، التنبيهات وwebhooks بالتعاون مع متطلبات المنتج والقانون. نسخ PostgreSQL وحدها لا تغطي ملفات MinIO أو إعدادات Firebase/FCM؛ لكل منها نسخ واختبار استعادة متوافق.
- تحديث نشر `infra/deploy/deploy-staging.sh`: migrations قبل تفعيل إصدار API متوافق، مع قاعدة expand/contract للتغيير غير القابل للتراجع آليًا. rollback كود PM2 الحالي لا يعيد مخطط DB، ولذلك يمنع نشر migration تكسر الإصدار السابق مباشرة.

## 10. قرارات مطلوبة عند بدء المرحلة 0

1. استضافة PostgreSQL: خادم خاص جديد أم خدمة مُدارة خاصة؛ البلد/المنطقة والميزانية الشهرية.
2. حجم البيانات الحالي، معدل النمو المتوقع، ومهلة توقف الكتابة المقبولة لكل مجال.
3. أهداف RPO/RTO النهائية، مدة الاحتفاظ بالنسخ، ومتطلبات الخصوصية لرسائل Chat وSMS.
4. ما إذا كانت هناك بيانات إنتاج حقيقية في Firestore يجب نقلها كاملة، وأي مجموعات تجريبية يجب استبعادها؛ يتم ذلك بجرد فعلي، لا بافتراضات الوثائق القديمة.

هذه القرارات لا تمنع بناء أدوات الجرد وDDL وبيئة staging، لكنها تمنع اعتماد حجم الخادم وموعد قطع الإنتاج.

## 11. قائمة العمل في المستودع

| مكان العمل | التغيير المطلوب |
| --- | --- |
| `apps/api/src/config/env.ts` | تعريف إعدادات PostgreSQL والتأكد من فصلها بين البيئات؛ يصبح مسار Firebase مطلوبًا فقط عند تشغيل FCM، لا عند اختيار PostgreSQL. |
| `apps/api/src/server.ts` | بناء pool وStores بحسب إعداد البيئة، ثم حصر تهيئة Firebase في FCM عند انتهاء النقل. إبقاء الخدمات والعقود كما هي. |
| `apps/api/src/{auth,chat,contacts,social,market,notifications,presence,calling,billing,telephony}/` | إضافة `postgres-*-store.ts` لكل interface موجود، مع اختبارات التكامل والسباق والـcursor المناسبة. |
| `apps/api/migrations/` و`apps/api/scripts/` | SQL مرقم للأمام، أداة migrator قابلة للإعادة والتحقق، وأوامر backup/restore غير محتوية على أسرار. |
| `apps/api/test/` | اختبارات العقد بين Store القديم والجديد، واختبارات PostgreSQL حقيقية للجلسات وChat وBilling/webhooks. |
| `infra/` وGitHub Actions | تجهيز قاعدة البيانات والنسخ والمراقبة؛ إضافة بوابة migrations متوافقة مع rollback الإصدار السابق، وفحوص health وsmoke؛ حفظ الأسرار على الخوادم خارج Git. |
| `docs/API_CONTRACT_V1.md` وrunbooks التشغيل | توثيق أي تغيير مقصود في عقد v1، طريقة القطع والرجوع، معالجة فشل النسخ، واستعادة PostgreSQL وMinIO. |

لا يتطلب النقل وحده تغييرًا في `apps/mobile` أو `apps/web` ما دامت استجابات `/api/v1` وسلوكها ثابتين؛ أي تعديل عميل يظهر فقط من اختبار توافق فعلي أو مطلب منتج مستقل.
