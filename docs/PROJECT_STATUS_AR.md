# حالة مشروع g000st وتسليم الجلسة

**تحديث قرار 25 سبتمبر 2026:** وافق صاحب المشروع على التخطيط لترحيل قاعدة بيانات المنتج إلى PostgreSQL والإبقاء على Firebase للإشعارات الضرورية؛ راجع [خطة الترحيل](POSTGRESQL_MIGRATION_PLAN_AR.md). إشارات Firestore أدناه تصف الحالة التي كان المشروع عليها عند تدوينها.

آخر تحديث: 23 سبتمبر 2026

هذا الملف هو نقطة البداية لأي جلسة جديدة. يجب قراءته مع `AGENTS.md` قبل تنفيذ أي تعديل.
لا تُسجل هنا قيم الأسرار أو المفاتيح أو بيانات المستخدمين لأن المستودع عام حاليًا.

## الملخص التنفيذي

- جُمعت مشاريع الويب والهاتف والباك في monorepo واحد داخل `/Volumes/wold/projects/g000st`.
- فرع العمل الأساسي هو `main`، وآخر commit تشغيلي قبل ملف التسليم هو `bec383d`.
- الريموت ما زال باسم `g000st-ios`، والمستخدم سيعيد تسميته إلى `g000st` لاحقًا.
- الملفات القديمة محفوظة في الفرع `archive/pre-monorepo-2026-09-16` وليست موجودة في `main`.
- `apps/api` هو الباك المعتمد. لا توجد خطة لإعادة بنائه من الصفر؛ نوسّعه ونصححه فقط عند الحاجة.
- نُفّذ تدفق الدخول/التسجيل الموحد على الويب وExpo، ونُشر أحدث إصدار ويب على staging بنجاح.
- أُنشئ GitHub Action يدوي لنشر `web` أو `api` أو `both` إلى staging مع فحوصات وrollback.
- مشكلة `package-lock.json` القديمة محلولة فعليًا، ونشر `api`/`both` نجح عدة مرات على staging (تفاصيل
  في قسم GitHub Action أدناه).
- **محدَّث 20 سبتمبر**: Chat، Social، ID/Profile، وContacts (الأربعة الأولى من الخارطة) منجزة
  كشرائح حقيقية كاملة في الباك والهاتف والويب، ومنشورة على staging (آخر commit منشور: `da39c12`).
  صورة المستخدم الشخصية تظهر الآن فعليًا في منشورات Social وفي هيدر Chat على المنصتين. المتبقي
  قبل اعتبارها "مغلقة" نهائيًا هو QA يدوي بحسابين حقيقيين لم يُنفَّذ بعد.
- التالي في الخارطة: تاب **Mobile** (اتصال، شحن رصيد، SMS) — أنظر القسم #6 والقيود المهمة فيه
  (لا مفاتيح مزوّد في العميل، لا اختيار مزوّد نهائي بدون موافقة المستخدم، مراجعة IAP قبل البيع
  داخل التطبيق).
- **محدَّث 22 سبتمبر**: نُفّذ في الباك فقط (بدون ربط عميل بعد) وحدة `apps/api/src/telephony/`
  الكاملة — Telnyx SMS الصادر ومكالمات خارجية صادرة عبر Call Control، مع توثيق Ed25519 للـwebhooks،
  idempotency، وربطها بوحدة billing الموجودة مسبقًا (`debitForCall`/`debitForSms`). التفاصيل في
  قسم "تحديث Telephony" أدناه.
- **محدَّث 23 سبتمبر — Telnyx/Stripe صار فعليًا LIVE على staging**: كل مفاتيح Telnyx وStripe (وضع
  اختبار) أُضيفت للسيرفر، الكود اتنشر، وتأكدنا فعليًا (`curl` مباشر) إن `/api/v1/billing/skus`
  و`/api/v1/telephony/sms` يرجعان `401` مو `404` — يعني الوحدتين شغّالتين حقًا، مو بس مكوّدتين.
  إعداد Telnyx (Connection + Messaging Profile) والـwebhooks كلها انضبطت من داخل الداشبورد
  (تفاصيل كاملة وIDs في ذاكرة `g000st-telnyx-stripe-config`، فيها فخ مهم: لازم API version يكون
  v2 يدويًا، الافتراضي v1). لوحة أدمن للفوترة (`/billing`) صارت حقيقية كمان. **الجلسة طالت جدًا
  وانتهت هنا** — أي جلسة جديدة تبدأ من قراءة ذاكرة `g000st-mobile-tab-progress` و
  `g000st-telnyx-stripe-config` أول شي، فيهم كل التفاصيل التقنية والفخاخ المكتشفة (تعارض
  react-native-webrtc، باگ dependencies بحزمة Telnyx، إلخ) بدل إعادة اكتشافها من الصفر. المتبقي
  بعد هذي الجلسة: مفتاح APNs VoIP (منفصل تمامًا عن Telnyx/Stripe)، كومت شغل الموبايل غير
  المحفوظ، وبناء iOS فعلي لم يُختبر.
- لم تُمس نسخة الإنتاج على `g000st.com`.

## تحديث جلسة 19 سبتمبر — Chat وExpo 56

تم تنفيذ الجزء الوظيفي الأساسي من تبويب Chat في commit `7163e37`
(`feat: implement push notification system with Expo integration`) وهو موجود على `main` والريموت:

- محادثة خاصة بين شخصين فقط، مع تحقق عضوية الباك في كل عملية.
- Pagination ثابتة للرسائل عبر cursor مركب من الوقت وID، بما في ذلك الرسائل ذات التوقيت المتساوي.
- الرسائل محفوظة ساعتين ثم تحذف فعليًا، ورسالة Burn تقفل عند المستلم وتُحذف بعد 5 ثوانٍ من فتحها.
- unread count و`firstUnreadMessageId`، والانتقال لأول غير مقروء في الويب والهاتف، وتصحيح المؤشر
  والعداد إذا انتهت صلاحية رسالة أقدم.
- حالة المرسل المرئية: `Sent` / `Read` للرسائل العادية و`Sent` / `Opened` لرسائل Burn.
- عند تعطيل/حذف الطرف الآخر يبقى السجل المحتفظ به قابلًا للقراءة، وتظهر `Deleted account` ويُمنع الإرسال.
- تسجيل أجهزة Expo Push لدى الباك، إلغاء التسجيل عند تسجيل الخروج، إشعار عام لا يحوي محتوى الرسالة،
  وفتح المحادثة المطلوبة عند الضغط على الإشعار.

التحقق المنفذ محليًا قبل آخر تعديلات lockfile:

- `npm run test:api`: ناجح، 15 اختبارًا.
- `npm run typecheck:api` و`npm --prefix apps/api run typecheck:tests`: ناجحان.
- `npm run typecheck:mobile` و`npm run lint:mobile`: ناجحان.
- `npm run typecheck:web` و`npm run build:web` و`npm run build:api`: ناجحة.
- Expo Doctor: 21/22 ناجح. التحذير الوحيد هو Hermes V1 المعروف في Expo SDK 56؛ علاجه يتطلب
  Expo SDK 57، وهذا غير مطلوب حاليًا لأن قرار المشروع هو البقاء على SDK 56.

Push لا يمكن تأكيده end-to-end بعد حتى يُوفَّر `EXPO_PUBLIC_EAS_PROJECT_ID` وتُضبط بيانات FCM v1
لأندرويد وAPNs لـiOS في EAS، ويُختبر على جهاز حقيقي/Development Build. لا يعمل remote push على
Expo Go في Android.

## تحديث جلسة 20 سبتمبر — Social مكتمل خلفيًا، ونشر جزئي، وTab Bar متحرك (عمل غير محفوظ)

نُفّذت شريحة Social كاملة تقريبًا في الباك بين commits `d766404` حتى `dcf8e1d` (يوم 20 سبتمبر):

- Endpoints حقيقية تحت `/api/v1/social`: `posts` (CRUD)، `like`، `comments` (CRUD)، `uploads`،
  `camp`، `profiles/:publicId` (قراءة)، `profile` (تحديث، PUT)، `alerts`، `alerts/read`، `reports`.
  جميعها بـcursor pagination وrate limiting وصلاحيات مالك، واختبارات `social-cursor` و
  `social-policy` مضافة.
- حقول الملف الاجتماعي الاختياري في `PUT /api/v1/social/profile`: `displayName`, `avatarUrl`,
  `country`, `age`, `sex`, `hobby`, `bio` — **هذه القيم نفسها تقريبًا المطلوبة لملف ID/Profile
  العام في القسم 4 من الخارطة**؛ يجب اتخاذ قرار في الجلسة القادمة: نعيد استخدام هذا الـendpoint
  لملف ID العام أيضًا، أم نبني endpoint مستقل؟ الأقرب لمبدأ "مصدر حقيقة واحد" هو إعادة الاستخدام
  إذا كانت الحقول متطابقة فعليًا.
- تاب Social مضاف في `_layout.tsx` للهاتف ومتصل بـAPI حقيقي (ليس mock)، مع دمج
  `react-native-toast-message` في `dcf8e1d`.
- صفحة الويب `/social` عُدّلت (`ff42883`, `dcf8e1d`) وتستخدم API حقيقي أيضًا، لكنها لم تُختبر QA
  بعد بحسابين حقيقيين.
- **خطأ مصحَّح في `a091391`** (اسم Firestore collection group من `social_posts` إلى
  `staging_v1_social_posts`) — **الفهرس أُنشئ يدويًا من طرف المستخدم عبر Firebase Console
  (21 سبتمبر) وSocial يعمل فعليًا الآن.** ملاحظة: هذا commit لم يغيّر كود `apps/api` أصلًا (فقط
  ملف الفهرس وتنسيق بسيط بالموبايل)، لذلك عدم نشره عبر GitHub Action لم يكن يمنع العمل من الأساس.

**عمل غير محفوظ في commit الآن (working tree)**: ملف جديد
`apps/mobile/src/components/navigation/animated-tab-bar.tsx` وتعديل على
`apps/mobile/src/app/(app)/(tabs)/_layout.tsx` يستبدل أيقونات التاب الثابتة بمكوّن tab bar
متحرك. يجب إنهاؤه واختباره وحفظه بـcommit قبل البدء بأي ميزة جديدة (مثل تاب ID)، لتجنّب تضارب لاحق.

## حالة GitHub Action والنشر — محدّثة ومؤكدة عبر `gh run list`

مشكلة `package-lock.json` المذكورة سابقًا (نسخ خاطئة لـ`react-native-gesture-handler`,
`react-native-reanimated`, `react-native-worklets`, `react-native-screens`) **محلولة فعليًا**.
تحقق مباشر من `package-lock.json` الحالي يؤكد النسخ الصحيحة (`2.31.2`, `4.3.1`, `0.8.3`,
`4.26.2`). لا حاجة لإعادة تنفيذ خطوات الإصلاح المذكورة سابقًا.

سجل التشغيلات الفعلي (`gh run list --workflow=deploy-staging.yml`) يؤكد نجاح النشر عدة مرات
بعد الإصلاح، بما فيها اختيار `api` منفردًا واختيار `both`:

| Run           | النتيجة | الاختيار | commit المنشور |
| ------------- | ------- | -------- | -------------- |
| `35400777304` | success | both     | `f81e0aa`      |
| `35437284469` | success | both     | `c3c6c49`      |
| `35474814517` | success | both     | `ff42883`      |
| `35476569888` | success | both     | `1731fd6`      |
| `35477178838` | success | **api**  | `e12c0ab`      |

آخر تشغيل ناجح نشر commit `e12c0ab` فقط. الـcommits التالية — `a091391` (فهرس Firestore فقط، لا
كود API) و`dcf8e1d` (toast بالموبايل + تعديل صفحة أدمن الويب + lockfile) — **لم تُنشر بعد عبر
GitHub Action**، لكن هذا غير مانع لأن Social يعمل فعليًا (الفهرس طُبِّق يدويًا، ولا تغيير API في
تلك commits). يُفضَّل تشغيل `Deploy staging` (اختيار `both`) عند الفرصة القادمة فقط لمزامنة
staging مع `main`، وليس كحاجة عاجلة.

تغييرات Axios أصبحت commits مرفوعة بالفعل: `f81e0aa` و`7f43577`. يمكن مراجعتها لاحقًا، لكنها
ليست تغييرًا محليًا متروكًا ولا يجب خلطها مع أي إصلاح لاحق.

## بنية المستودع

```text
apps/
  api/       Node.js API المشترك
  mobile/    Expo SDK 56 لتطبيق iOS وAndroid
  web/       Next.js للمستخدمين والأدمن
docs/        العقود والخطط وحالة المشروع
infra/       Nginx وسكربتات النشر بلا أسرار
```

المصادر القديمة التي سلّمها العميل (`admin.html` و`wweindex.html` و`appfg000st.html` والباك
القديم) محفوظة في فرع الأرشيف. مصدر حقيقة تطبيق الهاتف الأصلي هو `appfg000st.html`، أما الويب
القديم والأدمن فهما مرجعان للمطابقة البصرية والسلوكية فقط.

## القرارات المؤكدة مع المستخدم

- التطبيق خاص بمحادثات فردية فقط؛ لا توجد غرف أو محادثات جماعية.
- الخدمات مطلوبة كخدمات حقيقية وليست واجهات وهمية.
- تسجيل الدخول يتم بواسطة ID بطول 50 محرفًا.
- الهوية ستُقسم أمنيًا إلى Public ID قابل للمشاركة وRecovery ID سري للاستعادة؛ لا يجوز أن يكونا
  القيمة نفسها.
- الحذف في المرحلة الحالية من الجهاز فقط، ويجب مناقشة السلوك النهائي قبل تنفيذه فعليًا.
- لا توجد نسخة إنتاجية معتمدة حاليًا للتحويل المباشر.
- تطبيق الهاتف يستخدم Expo SDK 56 وTypeScript و`tw` بدل `StyleSheet` قدر الإمكان.
- المطلوب تطابق الويب والهاتف في التصميم والوظائف والسيناريو، مع مراعاة اختلافات المنصة
  الضرورية فقط.
- الباك والويب وقاعدة البيانات على الخادم الأساسي، والخادم الثاني مخصص مستقبلًا للميديا بعد
  تجهيز تخزين مناسب ونسخ احتياطي.
- Firestore باقية في المرحلة الحالية. قرار PostgreSQL منفصل ويؤجل إلى ما بعد استقرار المنتج.

## معايير التنفيذ المطلوبة

طلب المستخدم الالتزام بالمراجع التالية أثناء التنفيذ عندما تنطبق على المهمة:

- Axios: `/Users/moudy/.claude/skills/setup-axios/SKILL.md`
- تنظيف المكونات: `/Users/moudy/.claude/commands/refactor-component.md`
- توحيد الفورمز: `/Users/moudy/.claude/commands/form-input.md`
- كيبورد Expo: `/Users/moudy/.claude/skills/expo-keyboard-avoiding/SKILL.md`

يجب كذلك الحفاظ على TypeScript، وتقسيم المكونات الكبيرة، وعدم تكرار منطق الشبكة أو التحقق بين
الشاشات.

## خارطة التنفيذ المستمرة للوصول إلى منتج عامل بالكامل

### منهج العمل الملزم

نعمل على الميزة كشريحة مكتملة، لا على مجموعة شاشات منفصلة: نحدد السيناريو وعقد API، ثم نبني
الخدمة والتحقق والصلاحيات في `apps/api`، ثم Expo في `apps/mobile`، ثم الويب في `apps/web`، ثم
أدوات الأدمن اللازمة، ثم الاختبارات وQA والنشر إلى staging. لا نترك أزرارًا تعمل محليًا فقط ولا
ننشئ بيانات mock في ميزة يُفترض أن تكون حقيقية.

كل ميزة جديدة يجب أن تحقق الآتي قبل الانتقال للتي بعدها:

1. تصميم وسيناريو مطابقان لمرجع العميل قدر الإمكان.
2. مصدر حقيقة واحد في الباك؛ الويب والهاتف لا يكرران قواعد الصلاحيات أو الحسابات.
3. loading وempty وoffline/error وretry وحالات الحذف/التعطيل واضحة.
4. TypeScript وعقد request/response صريحان، وفحوصات مناسبة للباك.
5. تجربة يدوية بين مستخدمين حقيقيين على الويب والهاتف قبل اعتبار الشريحة مكتملة.

### ترتيب التنفيذ المعتمد

#### 0. تثبيت البنية والنشر

- يحافظ `apps/api` على كونه الباك الوحيد المعتمد، وFirestore هي قاعدة البيانات الحالية.
- يبقى API والويب وقاعدة البيانات على الخادم الأساسي؛ الخادم الثاني لا يستخدم قبل تجهيز Object
  Storage متوافق S3، volume، صلاحيات، نسخ احتياطية، وسياسة روابط موقعة للميديا.
- يتم النشر إلى staging يدويًا فقط من GitHub Action، ثم يختبر قبل لمس الإنتاج.
- الأولوية الفورية: إصلاح `package-lock.json` وإعادة تشغيل نشر API حتى تصبح ميزات Chat الموجودة
  على `main` موجودة فعليًا على staging.

#### 1. Auth والهوية والجلسة — منجز أساسيًا

- تسجيل الدخول بحقل واحد ذي 50 محرفًا، والتسجيل من زر منفصل مع تأكيد ثم نسخ ID الجديد.
- `Public ID` للمشاركة وإضافة الآخرين؛ `Recovery ID` سرّ مستقل للاستعادة ولا يظهر في الملف العام.
- الجلسات محفوظة آمنًا على الجهاز، وRefresh/Logout يعملان عبر الباك.
- المتبقي هنا: QA كامل على staging للويب والهاتف، وإدارة أجهزة/جلسات المستخدم ضمن Profile لاحقًا.

#### 2. Chat الخاص — بُني محليًا وينتظر نشر API وQA

- محادثة فردية فقط؛ لا Groups ولا غرف عامة.
- إنشاء/فتح المحادثة بـPublic ID، إرسال نصي، retry وidempotency، pagination، unread/read، والانتقال
  لأول رسالة غير مقروءة.
- احتفاظ الرسائل ساعتين وحذف فعلي؛ Burn اختياري يفتح عند المستلم ثم يحذف بعد خمس ثوانٍ.
- تظهر حالة الطرف المحذوف مع بقاء السجل المحتفظ به ومنع الإرسال إليه.
- Push للهاتف مدمج لكن لا يعد جاهزًا فعليًا قبل EAS Project ID وFCM/APNs واختبار جهاز حقيقي.
- لا يرى الأدمن محتوى المحادثات، ولا يوصف النظام بأنه E2EE قبل تنفيذ ذلك وتدقيقه.
- المتبقي قبل إغلاق هذه الشريحة: نشر API، اختبار هاتف ↔ ويب بحسابين، اختبار pagination وBurn
  والحذف والقراءة، ثم قرار realtime بدل polling المؤقت وميزات الرد/التفاعل/المرفقات لاحقًا.

#### 3. Contacts — منجزة ومنشورة على staging (20 سبتمبر)

**محدَّث**: شريحة أولى حقيقية منجزة (باك + موبايل + ويب)، محفوظة بـcommit `da39c12` ومنشورة
فعليًا (`Deploy both`، run `35536858976`، ناجح):

- وحدتان جديدتان في `apps/api/src`: `contacts` (إضافة/حذف/سرد جهات اتصال حقيقية، Firestore) و
  `presence` (heartbeat + حساب online، نافذة دقيقتين). `ContactsService` يعيد استخدام
  `SocialService.getProfile` لجلب الاسم/الصورة — لا تكرار لمنطق الأفاتار.
- إضافة جهة اتصال بـPublic ID فقط حاليًا (QR مؤجَّل عمدًا لمرحلة لاحقة — قرار المستخدم).
- تابا **All** و**Online** فقط يعملان فعليًا. **تاب Favorites أُزيل تمامًا ولن يُبنى حاليًا** —
  قرار صريح من المستخدم ("لن ننهيها الآن أبدًا")، وليس نطاقًا مؤجَّلًا فقط بل مُستبعد من الواجهة.
- Online حقيقي عبر heartbeat: الموبايل والويب يرسلان نبضة كل 45 ثانية طالما التطبيق بالمقدمة
  (AppState على الموبايل، visibilitychange على الويب)، والباك يعتبر المستخدم online إذا كانت آخر
  نبضة قبل أقل من دقيقتين — تلقائي بدون حدث "خرج من التطبيق" صريح.
- Remove = حذف حقيقي فوري في الباك للعلاقة من جهة المستخدم فقط (لا يمس الطرف الآخر ولا
  المحادثة) — مختلف تمامًا عن block المؤجَّل.
- فتح Chat من الصف يفتح/يبدأ المحادثة الحقيقية الموجودة (نفس آلية Social).
- **جانبي أثناء هذا العمل**: اكتُشف أن صورة الناشر في منشورات Social لم تكن تظهر أبدًا (خلل في
  `firestore-social-store.ts` كان يقرأ حقل Firestore قديم `avatarUrl` بعد أن تغيّر الاسم إلى
  `avatarObjectKey`)؛ صُحِّح، وأصبحت الصورة الحقيقية تظهر في منشورات Social **وفي هيدر صفحة
  Chat** (صورة الطرف الآخر عند فتح محادثة، بنفس placeholder الافتراضي في Social) على المنصتين.
- جهات اتصال الهاتف منفصلة تمامًا عن Contacts الخاصة بـg000st، ولا تطلب صلاحيتها إلا عند استخدامها.
- المتبقي: QR (لاحقًا حسب القرار)، وQA يدوي كامل بحسابين حقيقيين على staging (لم يُنفَّذ بعد
  رسميًا رغم أن كل الفحوصات الآلية ناجحة والنشر تم).

#### 4. ID & Profile — بعد Contacts (القادمة الآن؛ Contacts لم تُبنَ بعد — تجاوزها قرار المستخدم)

**تحديث 21 سبتمبر — نُفّذت شريحة أولى حقيقية (غير محفوظة بـcommit بعد):**

- **الباك**: أُضيف رفع صورة حقيقي عبر MinIO بنفس نمط الإرفاق المستخدم في Social (`POST
/api/v1/social/avatar-uploads` ثم `PUT /api/v1/social/profile` مع `avatarMedia`)، بحد 3MB
  وصور فقط. التخزين الداخلي أصبح `avatarObjectKey` ويُحوَّل إلى رابط موقّع صالح 30 دقيقة عند كل
  قراءة للبروفايل (`getProfile`/`updateProfile`) — القرار المتخذ: **إعادة استخدام
  `/api/v1/social/profile` لملف ID العام** (نفس الحقول تقريبًا)، دون لمس عرض الصور بجانب المنشورات
  (خارج النطاق، ولا تراجع لأنها لم تكن مفعّلة أصلًا).
- **الموبايل والويب**: أُعيد بناء شاشة/صفحة ID بالترتيب المطلوب: صورة أولًا (قابلة للتغيير) ← اسم
  ← بطاقة Public ID (نسخ/مشاركة) ← بطاقة Recovery ID (توضيحية فقط، بلا قيمة معروضة لأنه سرّ
  لا يُخزَّن كنص واضح ولا يمكن استرجاعه أصلًا — التحقق منه فقط عبر hash) ← حقول اختيارية
  (بلد/عمر/جنس/هواية/نبذة) ← حفظ ← تسجيل خروج (أصبح بتأكيد modal على المنصتين، لم يكن موجودًا
  بالموبايل سابقًا). التصميم على الويب أصبح مطابقًا بصريًا للموبايل (بطاقات رمادية/أحمر) بدل
  الاعتماد على نمط Social الأبيض المسطّح.
- **حُذفت من صفحة الويب القديمة عمدًا** (لم تُحوَّل mock→حقيقي لعدم وجود باك فعلي لها): بطاقة
  Ringtones، Contact us/Support، وروابط Legal. قرار مقصود لتفادي إبقاء أزرار وهمية، لكنه يحتاج
  تأكيد المستخدم إن كان يريدها كخدمات حقيقية لاحقًا أو استبعادها نهائيًا من النطاق.
- التحقق: `typecheck:api`, `test:api` (19/19), `build:api`, `typecheck:mobile`, `lint:mobile`,
  `typecheck:web`, `build:web` كلها ناجحة. **لم يُشغَّل تشغيل حي على المحاكي/المتصفح بعد** لتجربة
  رفع صورة فعلية على staging.
- المتبقي: تجربة حية (upload صورة فعلي، حفظ حقول، تسجيل خروج) على الموبايل والويب معًا، ثم commit،
  ثم القرار بخصوص Support/Legal، ثم إدارة الجلسات/الأجهزة (غير مبنية، لا يوجد endpoint حاليًا
  لعرض/إبطال جلسات — تسجيل الخروج محلي فقط ولا يُبطل refresh token على الباك).

**حالة فعلية مؤكدة بقراءة الكود في 20 سبتمبر — الميزة غير متطابقة بين المنصتين وغير مبنية كشريحة:**

- **الباك**: لا يوجد route مخصص لـ`/users` أو `/profile` عام في `apps/api/src` (الموجود فقط:
  `auth`, `chat`, `social`, `media`, `notifications`). القيم الجاهزة أصلًا: `publicId`/`recoveryId`
  من خدمة auth (`apps/api/src/core/secrets.ts`)، و`PUT /api/v1/social/profile` الذي يغطي تقريبًا
  نفس الحقول الاختيارية المطلوبة هنا (`displayName`, `avatarUrl`, `country`, `age`, `sex`, `hobby`,
  `bio`). لا توجد بنية لإدارة الجلسات/الأجهزة، الدعم، البلاغات، أو صفحات Privacy/Terms حقيقية.
- **الهاتف** (`apps/mobile/src/features/identity/`): شاشة حقيقية صغيرة — Public ID فعلي من auth،
  نسخ حقيقي (clipboard)، Sign out حقيقي. لا يوجد Recovery ID معروض، لا صورة/ملف اختياري، لا دعم،
  لا إدارة جلسات.
- **الويب** (`apps/web/app/(user)/profile/page.tsx`، 305 سطر): واجهة مورّثة من الملف القديم لكنها
  **mock بالكامل**: البروفايل يُحفظ في `localStorage`، الصورة تُحوَّل إلى base64 محليًا بدون رفع
  حقيقي (يخالف قاعدة "لا تُستخدم روابط أو تخزين محلي كبديل لخدمة حقيقية")، رسالة الدعم مزيفة
  بـ`setTimeout`، Privacy/Terms مجرد toast. لا نداء API واحد حقيقي في هذه الصفحة عدا قراءة
  `publicId` من session.
- **قرار مطلوب قبل البناء**: إعادة استخدام `/api/v1/social/profile` لملف ID العام (نفس الحقول
  تقريبًا) بدل بناء endpoint مستقل، أم فصلهما فعليًا إذا كان القصد ملفين مختلفين (عام مقابل
  اجتماعي)؟ يجب حسم هذا مع المستخدم قبل الترميز.

المطلوب بناؤه كشريحة كاملة:

- عرض ونسخ ومشاركة Public ID وQR، وإظهار Recovery ID فقط في تدفق آمن مخصص إذا تقرر ذلك.
- ملف اختياري: الاسم والصورة والبلد والعمر والجنس والهواية والاهتمامات وفق الحقول النهائية.
- إدارة الجلسات/الأجهزة، Logout، الخصوصية، السياسات، الدعم، والبلاغات كخدمات حقيقية.
- رفع الصور لا يذهب إلى Firestore أو base64؛ يستخدم media service وروابط موقعة (متوفر أصلًا لـSocial).
- حذف الحساب الفعلي قرار مؤجل؛ ما بُني في Chat يتعامل مسبقًا مع حالة حساب غير نشط عند توفرها.
- إعادة بناء صفحة الويب من الصفر وظيفيًا (إزالة mock بالكامل)، وتوسيع شاشة الهاتف لتطابقها.

#### 5. Social — بعد Profile

Social ليست محادثة جماعية. هي Feed للمنشورات، وأي مراسلة من منشور تستخدم Chat الخاص نفسه.

- الواجهة المرجعية: Home Feed وMy Page وAlerts، مع زر إنشاء منشور وcomposer باسم مستعار افتراضيًا.
- المنشور: نص وصورة، نشر، تعديل وحذف للمالك، Like، Comment، Share/Copy link، Report، وفتح ملف الناشر.
- Follow أو `Camp`: نثبت الاسم والسلوك النهائيين قبل بناء API.
- ملف اجتماعي اختياري: صورة، بلد، عمر، جنس، هواية، ونبذة، مع إعدادات لإظهار الاسم/الهوية أو البقاء
  مجهولًا حسب سياسة الخصوصية.
- الإشعارات: likes/comments/follow عبر خدمة حقيقية، منفصلة عن إشعارات الرسائل.
- المكان المعتمد للحفاظ على المرجع: في الهاتف شاشة مستقلة خارج التابات الأساسية تُفتح من زر
  `g000st Social` داخل Chat؛ وفي الويب المسار `/social` من المدخل نفسه. لا نضيف Tab خامسًا.
- `Social Chat` القديم لا يبقى نظام رسائل مستقلًا: الضغط على Chat من منشور يفتح `/chat`/محادثة
  المستخدم الخاصة الموجودة، ثم يزال منطق الرد الآلي وبيانات `/social-chat` التجريبية.
- **محدَّث 20 سبتمبر**: Social API حقيقي منجز ومنشور جزئيًا (انظر قسم "تحديث جلسة 20 سبتمبر"
  أعلاه)؛ تاب Social موجود فعليًا في Expo ومتصل بالـAPI، وصفحة الويب `/social` متصلة بالـAPI
  أيضًا (لم تعد mock). `/social-chat` لا يزال يعيد التوجيه إلى `/social`. الناقص: moderation
  حقيقية للأدمن، ونشر فهرس Firestore، وQA كامل.
- نماذج بيانات الباك المطلوبة: `posts`, `comments`, `reactions`, `follows`, `reports`, وalerts
  المرتبطة بها، مع cursor pagination، صلاحيات المالك، rate limits، وواجهة أدمن للمراجعة.

#### 6. Mobile: calls وSMS والأرقام والدفع — بعد Social

- جهات هاتف، Favorites، Recents، Keypad، رقم فعلي من مزود يختاره العميل، SMS وCalls حقيقية عبر الباك
  وwebhooks موثقة.
- لا يوضع مفتاح Telnyx/Twilio أو أي مزود في Expo أو الويب.
- الرصيد والاستهلاك وentitlements تحسب في الباك فقط، مع ledger وأحداث قابلة للتدقيق.
- الدفع: Stripe للويب؛ مراجعة Apple/Google In-App Purchase قبل بيع الخدمات الرقمية داخل الهاتف.
- لا ننفذ مزود المكالمات أو الأسعار أو المنتجات النهائية قبل موافقة المستخدم عليها.

#### 7. الأدمن — يواكب كل شريحة ولا يقرأ المحادثات

- مصادقة وصلاحيات Admin مستقلة ومدققة، مع audit log للأفعال الحساسة.
- مستخدمون حقيقيون: بحث، تعليق/إعادة تفعيل، وحالة الحساب من مصدر الباك.
- إدارة Reports والمحتوى والدعم، ومدفوعات واستهلاك بلا مفاتيح أو Recovery IDs.
- في Social: مراجعة البلاغات والقرارات على المنشورات والتعليقات، لا قراءة رسائل Chat الخاصة.
- صفحات Users وSettings الحالية لا تعتمد كنسخة نهائية لأنها تتضمن بيانات mock.

### حدود المنتج الحالية والقرارات المؤجلة

- لا Groups، ولا Trading/Network/Whisper من `wweindex.html` ضمن النطاق الحالي.
- E2EE مؤجل؛ الخصوصية الحالية تعني حماية الوصول وعدم عرض المحتوى للأدمن، لا تشفيرًا طرفيًا كاملًا.
- الصور والملفات والصوت والموقع في Chat تؤجل حتى media service؛ لا تستخدم روابط أو تخزينًا محليًا
  كبديل لخدمة حقيقية.
- الحذف من الجهاز فقط حاليًا؛ حذف الحساب، block على الخادم، وسياسة الاحتفاظ النهائية تحتاج قرارًا.
- realtime يستبدل polling بعد استقرار API، مع إبقاء REST مصدر الحقيقة.

### نقطة استئناف العمل في أي جلسة جديدة

1. اقرأ `AGENTS.md` ثم هذا الملف و`docs/CHAT_IMPLEMENTATION_STATUS_AR.md`.
2. تحقق من working tree أولًا (`git status`) — قد يوجد عمل غير محفوظ من الجلسة السابقة.
3. Chat وSocial وID/Profile وContacts أصبحت شرائح حقيقية منجزة ومنشورة على staging (آخر commit
   `da39c12`)؛ الناقص هو QA يدوي بحسابين حقيقيين لكل منها، لم يُنفَّذ رسميًا بعد.
4. **التالي فعليًا هو تاب Mobile** (اتصال/شحن رصيد/SMS) — القسم #6 أدناه. لا تبدأ الكود قبل
   حسم قرار مزوّد الاتصال مع المستخدم صراحة، ومراجعة سياسة IAP لـApple/Google.
5. Favorites في Contacts مستبعدة نهائيًا بقرار المستخدم — لا تُبنى ولا تُقترح مجددًا بدون طلب صريح.
6. عند الوصول إلى Social استخدم المرجع الخاص:
   `/Volumes/wold/projects/g000st-private-backup-20260917/legacy-root/appfg000st.html`، لكن لا تنسخ
   localStorage/Firebase client-side أو مفاتيح/منطق غير آمن منه.

## ما نُفّذ في تجربة الدخول الموحدة

آخر تعديل وظيفي هو commit `8463cf1`:

- أُلغيت صفحة الهبوط كمسار مستقل.
- المستخدم غير المسجل يُوجّه إلى `/login`، والمسجل يدخل التطبيق.
- شاشة الدخول تحوي حقل ID واحدًا فقط.
- زر Register يفتح تأكيدًا قبل إنشاء حساب.
- بعد الإنشاء يظهر ID الجديد داخل modal مع زر نسخ.
- بعد النسخ يعود المستخدم ويضع ID في حقل الدخول.
- طُبق السيناريو نفسه على الويب وExpo.
- مسار `/register` في الويب يعيد التوجيه إلى تجربة الدخول الموحدة.
- الواجهتان تتصلان بباك staging الحقيقي وليستا mock.

ملفات الإعداد العامة تشير إلى API staging:

- الويب: `NEXT_PUBLIC_API_URL=https://staging.g000st.com` مع base path `/api/v1`.
- الهاتف: `EXPO_PUBLIC_API_URL=https://staging.g000st.com/api/v1`.

## حالة الباك

- مصدر الحقيقة الحالي: `apps/api`.
- الـAPI يعمل على staging خلف `https://staging.g000st.com/api/v1`.
- health endpoint المؤكد: `/api/v1/health` ويرجع `{"ok":true}`.
- المصادقة والجلسات موجودة، واختبار المصادقة الكامل على staging نجح سابقًا مع تنظيف حساب الاختبار.
- عقد المسارات موجود في `docs/API_CONTRACT_V1.md`.
- الباك الحالي يعتمد Firebase Admin وFirestore، وتوجد تكاملات Stripe وGroq.
- لا تُنشئ backend جديدًا موازيًا ولا تعتمد نسخة باك من مجلد قديم.

## حالة staging المؤكدة

- الويب: `https://staging.g000st.com`.
- زيارة `/` دون جلسة تعيد `307` إلى `/login`.
- `/login` يعيد `200`.
- API health ناجح.
- عملية الويب في PM2 حالتها `online`.
- أحدث release ويب نشط:
  `35107662675-1-bec383d7cdbc8aaf1e84c79b9fa18a28c57ba6b4`.
- نسخة الإنتاج والنطاق الرئيسي لم يتغيرا.

يمكن الوصول إلى الخادم الأساسي عبر SSH alias محلي باسم `g000st-web`. يوجد alias آخر باسم
`g000st` للخادم الثاني/القديم، ولا يجب افتراض أن الخادمين يؤديان الدور نفسه.

## GitHub Actions والنشر

الـworkflow موجود في `.github/workflows/deploy-staging.yml` واسمه `Deploy staging`.

- تشغيله يدوي عبر `workflow_dispatch`.
- الخيارات: `web` أو `api` أو `both`.
- يعمل من `main` فقط.
- عند اختيار `both` ينشر API أولًا ثم الويب.
- يشغّل typecheck/tests/build المناسبة قبل الرفع.
- لا يضع `.env` داخل حزم النشر.
- يبني release مستقلًا على السيرفر ويبدّل symlink ذريًا.
- يفحص الخدمة بعد النشر ويعمل rollback تلقائيًا عند الفشل.
- يستخدم حساب SSH مقيّدًا، وليس root أو حسابًا شخصيًا.

أول تشغيل حقيقي للويب نجح:

- GitHub run: `35107662675`
- commit: `bec383d7cdbc8aaf1e84c79b9fa18a28c57ba6b4`
- النتيجة: `success`
- المدة: دقيقتان و6 ثوانٍ.

بسبب عدم امتلاك المستخدم صلاحية GitHub Environments، أضيفت القيم حاليًا كـRepository secrets.
هذا عمل بنجاح. الأسماء المطلوبة فقط، من دون تسجيل قيمها، هي:

- `DEPLOY_SSH_HOST`
- `DEPLOY_SSH_USER`
- `DEPLOY_SSH_PRIVATE_KEY`
- `DEPLOY_SSH_KNOWN_HOSTS`

المنفذ الافتراضي داخل الـworkflow هو `22`، لذلك `DEPLOY_SSH_PORT` اختياري. عند توفر صلاحية
Environments لاحقًا يفضل نقلها إلى Environment باسم `staging` مع reviewer وحصر الفرع بـ`main`.

## الأمان والأرشفة

- المستودع عام؛ لا تضع فيه IP خاصًا غير ضروري أو مفاتيح أو ملفات `.env` أو Recovery IDs.
- نسخة خاصة من المواد القديمة والحساسة محفوظة في
  `/Volumes/wold/projects/g000st-private-backup-20260917` خارج المستودع بصلاحيات مقيدة.
- مفتاح GitHub Actions الخاص موجود في GitHub Repository secrets ولم يُرفع إلى Git. فُقدت النسخة
  المحلية أثناء نقل المشروع؛ إذا احتجنا نسخة محلية لاحقًا يجب تدوير المفتاح بدل محاولة استعادته.
- مستخدم النشر على الخادم لا يملك عبر sudo إلا تشغيل سكربت staging المحدد.
- الملفات القديمة القابلة للرجوع موجودة في فرع الأرشيف بدل بقائها مبعثرة في `main`.

## التشغيل محليًا

من جذر المشروع:

```bash
npm ci
npm run dev:web
npm run dev:mobile
```

لتشغيل Expo Web على منفذ مختلف عند انشغال 8081:

```bash
npm --prefix apps/mobile run web -- --port 8082
```

يمكن تشغيل الواجهتين محليًا مع باك staging ولا يلزم تشغيل API محليًا لهذا السيناريو.

## ما لم يكتمل بعد

### تحديث Social — 19 سبتمبر 2026

- غُيّر قرار التنقل: أصبحت `SOCIAL` التاب الخامس في الويب وExpo بدل فتحها من داخل Chat.
- أضيف نطاق باك حقيقي تحت `/api/v1/social` لمنشورات النص/الصورة، التعليقات، الإعجابات، Camp،
  الملفات الاجتماعية الاختيارية، alerts والبلاغات، مع cursor pagination وrate limits وصلاحيات المالك.
- إخفاء هوية المنشور/التعليق المجهول يطبق في استجابة الباك، وليس تجميلاً في الواجهة.
- رُبطت صفحتا الويب والهاتف بالـAPI وأضيف Home وMy Page وAlerts وفتح Chat الخاص الحقيقي من المنشور.
- أزيل تنفيذ `Social Chat` الوهمي؛ المسار القديم `/social-chat` يعيد التوجيه إلى `/social`.
- ميديا المنشورات تستخدم signed upload إلى MinIO على `g000st-app` ولا تستخدم base64 أو Firestore؛
  المسموح صورتان كحد أقصى أو فيديو واحد فقط، وكل ملف حتى 5MB.
- نجحت فحوص TypeScript للباك/الويب/الهاتف، وLint الهاتف، واختبارات API الحالية 15/15، وبناء الباك والويب.
- تم التأكد من ربط API staging بـ`media.g000st.com` والـbucket الخاص، وضُبط CORS المقيد في Nginx،
  ونجح اختبار signed PUT ثم HeadObject ثم حذف ملف الاختبار.
- **محدَّث 20 سبتمبر**: كود Social نُشر فعليًا حتى commit `e12c0ab` (انظر جدول النشر أعلاه)، لكن
  فهرس Firestore المصحَّح (`staging_v1_social_posts`) لم يُنشر بعد، وcommits `a091391`/`dcf8e1d`
  لم تُنشر إلى staging. المتبقي: نشر الفهرس، إعادة تشغيل Deploy staging على آخر commit، ثم QA
  بحسابين حقيقيين واختبار الصور والفيديو وanonymous/public وCamp والتعليقات والبلاغات.

- نشر `api` عبر GitHub Action **تم اختباره بنجاح فعليًا** (عدة تشغيلات ناجحة، انظر الجدول أعلاه)؛
  الملاحظة القديمة بأن مسار `web` فقط اختُبر لم تعد صحيحة.
- يجب إجراء QA يدوي كامل لتدفق Register → Copy ID → Login → Refresh → Logout على staging.
- يجب متابعة مطابقة صفحات Expo صفحة بصفحة مع `appfg000st.html` وربط كل الخدمات الحقيقية.
- يجب مراجعة الويب والأدمن لاحقًا وتحسين ما أنشئ سابقًا عند الوصول إلى كل جزء.
- خادم الميديا `g000st-app` يعمل عليه MinIO خلف Nginx وTLS، وAPI staging قادر على الوصول إلى
  bucket. المساحة الحالية نحو 38GB إجماليًا/29GB متاحًا؛ يلزم لاحقًا وضع سياسة نسخ احتياطي ومراقبة سعة.
- قرار حذف البيانات النهائي وقاعدة البيانات المستقبلية مؤجلان للنقاش.
- اسم مستودع GitHub لم يتغير بعد من `g000st-ios` إلى `g000st`.

## تحديث Telephony — 22 سبتمبر 2026 (SMS ومكالمات خارجية، باك فقط)

تنفيذ لبندي "5" و"6" من قسم Sequencing في خطة Mobile tab المحفوظة سابقًا
(`/Users/moudy/.claude/plans/buzzing-swimming-kitten.md`) — الجزء الخاص بـTelnyx تحديدًا، وليس
in-app calling (ذاك منجز بشكل مستقل مسبقًا). عقد الـAPI (`## Mobile API` في
`docs/API_CONTRACT_V1.md`) كان مكتوبًا مسبقًا بالكامل؛ هذا التحديث ينفّذه فعليًا.

- وحدة جديدة `apps/api/src/telephony/`: `telephony-router.ts` (REST)،
  `telephony-webhook-router.ts` (Telnyx webhooks)، `telephony-service.ts`، `telnyx-client.ts`
  (طبقة رقيقة فوق `fetch` بلا SDK خارجي)، `telnyx-webhook-verify.ts` (توقيع Ed25519 محقَّق عبر
  اختبار فعلي — الصيغة الموقَّعة هي `${timestamp}|${rawBody}` والمفتاح العام JWK OKP)،
  `firestore-telephony-store.ts` وأنواع/cursor مطابقة لأسلوب `billing`/`calling` الموجود.
- SMS: `POST/GET /api/v1/telephony/sms`، صادر فقط، عبر Telnyx Messaging API، مع خصم رصيد SMS
  واحد لكل رسالة من billing ledger الموجود مسبقًا.
- مكالمات خارجية — **تصحيح معماري مهم بتاريخ 22 سبتمبر (بعد المحاولة الأولى في نفس اليوم)**:
  التصميم الصحيح هو أن **المتصفح/الهاتف يتصل مباشرة عبر Telnyx WebRTC SDK**
  (`client.newCall({destinationNumber})`)، وليس عبر Call Control REST dial من الباك. اكتشفنا هذا
  أثناء بحث توثيق Telnyx WebRTC قبل ربط الواجهة: طلب REST dial من الباك كان سينشئ مكالمة حقيقية
  منفصلة لا تتصل بالمستخدم إطلاقًا. لذلك `POST /api/v1/telephony/calls` أصبح **فحص رصيد فقط**
  (`authorizeExternalCall`، بلا أي اتصال بـTelnyx)، وسجل المكالمة (`ExternalCall`) يُنشأ الآن
  تفاعليًا من webhook حدث `call.initiated` (وليس من الـREST endpoint). الفوترة عند `call.hangup`
  كما هي (مدة answered→hangup مقرَّبة لأعلى للدقيقة، idempotent على مستويين).
- **إصلاح أمني مرتبط بالتصحيح أعلاه**: بما أن العميل هو من يضع `clientState` عند طلب المكالمة عبر
  WebRTC SDK، كان يمكن لعميل خبيث انتحال Public ID مستخدم آخر وتحميل فاتورة المكالمة عليه. الحل:
  `POST /api/v1/telephony/webrtc-credential` الآن يُرجع `clientState` كـtoken موقَّع بـHMAC من
  الباك (باستخدام `AUTH_RECOVERY_PEPPER` نفسه، بفصل نطاقي domain-separated) بدل الـPublic ID
  الخام؛ العميل يمرره كما هو بلا فك أو إنشاء، والباك يتحقق من التوقيع قبل الثقة به عند أي webhook.
  اختبار مخصص جديد يتأكد من رفض `client_state` مزوَّر (`telephony-service.test.ts`).
- `POST /api/v1/telephony/webrtc-credential`: يُصدر بيانات دخول WebRTC قصيرة العمر عبر
  `/v2/telephony_credentials` + `/v2/telephony_credentials/{id}/token`. **تحذير**: صيغة استجابة
  الـtoken endpoint (نص خام JWT أم JSON) غير موثَّقة علنًا بالكامل من Telnyx وتحتاج تأكيدًا فعليًا
  بمجرد توفر `TELNYX_API_KEY` حقيقي — لم تُختبر ضد حساب Telnyx فعلي بعد.
- Config: `TELNYX_API_KEY`/`TELNYX_PUBLIC_KEY`/`TELNYX_CONNECTION_ID`/`TELNYX_SHARED_NUMBER_E164`
  في `apps/api/src/config/env.ts` (اختيارية معًا، وتتطلب أن يكون billing/Stripe مُفعَّلاً أيضًا) —
  موثقة في `.env.example`. لا مفاتيح Telnyx في Expo أو الويب أبدًا (يتطابق مع القيد المذكور أعلاه).
- إضافة فهرس Firestore مركّب جديد لـ`staging_v1_telephony_sms` في `firestore.indexes.json` (لم
  يُنشر على staging بعد — نفس الحالة التي واجهها فهرس `social_posts` سابقًا، يحتاج نشرًا يدويًا).
- اختبارات جديدة كاملة (`telephony-cursor`، `telnyx-webhook-verify`، `telephony-service`،
  `telephony-webhook-router`) — **73/73 ناجحة** إجمالاً بعد الإضافة (كانت 70 قبل التصحيح المعماري
  والأمني)، ونجح `typecheck:api`/`typecheck:tests`/`build:api`.

### تحديث فرعي — ربط الويب فعليًا (نفس اليوم، 22 سبتمبر)

بدأنا تنفيذ البند 9 من الخطة (ربط الويب) لأنه لا يحتاج EAS dev-client rebuild كالهاتف:

- `apps/web/app/api/mobile.ts` + `apps/web/features/mobile/types.ts`: طبقة API كاملة لـbilling
  وtelephony (SKUs، رصيد، ledger، credential، calls، sms).
- تبويب **SMS** في `apps/web/app/(user)/mobile/page.tsx`: إرسال/سجل حقيقي بالكامل، معالجة
  `INSUFFICIENT_BALANCE`.
- تبويب **Plans**: SKUs حقيقية من الباك، زر **Buy** واحد (حسب القرار المعتمد: لا أزرار Apple/Google
  Pay داخل التطبيق) يوجّه إلى Stripe Checkout، ورجوع إلى `/mobile?checkout=success|cancel` مع
  polling قصير للرصيد بدل الثقة بالـredirect وحده (كما يشترط العقد).
- تبويب **Keypad**: مكالمات خارجية حقيقية عبر `@telnyx/webrtc` (تثبيت جديد، `apps/web/features/mobile/use-external-call.ts`) — تسجيل SIP بالـcredential، `newCall`، حالة
  المكالمة (ringing/active/ended)، mute، hangup، عداد مدة المكالمة، شاشة تغطية أثناء المكالمة.
- `npm run typecheck:web` و`npm run build:web` ناجحان (حجم `/mobile` صار 78.3kB بعد تضمين SDK
  الـWebRTC، متوقع).
### تحديث فرعي — ربط الهاتف (Expo) فعليًا (نفس اليوم، 22 سبتمبر، مساءً)

تنفيذ البند 8 من الخطة (ربط الموبايل). قبل كتابة أي كود، اكتُشفت وحُلّت مشكلتان حقيقيتان بالبحث
والتحقق المباشر من الكود المثبَّت فعليًا (وليس تخمينًا من التوثيق):

- **تعارض WebRTC الأصلي**: `@telnyx/react-native-voice-sdk` يعتمد على حزمة `react-native-webrtc`
  الأصلية، بينما ميزة in-app calling الموجودة مسبقًا تستخدم `@livekit/react-native-webrtc`. تأكدنا
  عبر قراءة الكود الأصلي (Android) أن كلا الحزمتين تسجّلان نفس اسم native module
  (`com.oney.WebRTCModule.WebRTCModule`) — تعارض بناء حقيقي مؤكد وليس افتراضًا. **القرار (وافق
  عليه المستخدم صراحة)**: إضافة `"react-native-webrtc": "npm:@livekit/react-native-webrtc@^144.2.0"`
  إلى `overrides` في `package.json` الجذر، بحيث تُحلّ كل استيرادات `react-native-webrtc` إلى نفس
  حزمة LiveKit المثبّتة والمُختبرة مسبقًا. تحقّقنا عبر `npm ls` أن هناك native module واحد فقط الآن.
- **حزمة Telnyx نفسها ناقصة dependencies حقيقية**: قراءة مصدر الحزمة المثبّتة (v1.1.0، وهي أحدث
  إصدار متاح) أظهرت أنها تستورد فعليًا `@react-native-async-storage/async-storage` و`rxjs` وقت
  التشغيل، لكن `package.json` الخاص بها لا يذكرهما ضمن `dependencies` إطلاقًا — كانت ستفشل
  التطبيق فعليًا عند التشغيل (`Unable to resolve module`) لو لم نُضفهما يدويًا. أُضيفتا صراحةً إلى
  `apps/mobile/package.json` (`@react-native-async-storage/async-storage` عبر `npx expo install`
  للحصول على النسخة المتوافقة مع SDK 56، و`rxjs@^7`).
- **مشكلة typecheck منفصلة**: نفس حزمة Telnyx تشحن كود `.ts` خام غير مُصرَّف كنقطة `types`/`main`،
  وهذا الكود لا يمر فحص TypeScript الصارم لهذا المشروع (أخطاء null-safety وRTCStatsReport داخلية
  في الحزمة نفسها). الحل: `apps/mobile/tsconfig.typecheck.json` جديد (يُستخدم فقط بواسطة سكربت
  `npm run typecheck`، **لا يمسّه Metro إطلاقًا** الذي يستخدم `tsconfig.json` الأصلي) يعيد توجيه
  استيراد `@telnyx/react-native-voice-sdk` عبر `paths` إلى stub محلي مكتوب يدويًا
  (`src/types/telnyx-react-native-voice-sdk-stub.ts`) يطابق الواجهة الحقيقية المُتحقَّق منها من
  الكود المصدري فقط — التطبيق الفعلي (Metro) يستمر باستخدام الحزمة الحقيقية بلا أي تغيير.
- وحدة جديدة `apps/mobile/src/features/mobile/`: hooks (`use-mobile-external-call.ts`،
  `use-mobile-balance.ts`، `use-mobile-sms.ts`، `use-mobile-checkout.ts`) + مكوّنات
  (`external-call-overlay.tsx`، `sms-composer-modal.tsx`، `plans-modal.tsx`) + `src/api/mobile.ts`
  و`src/domain/mobile/types.ts` (zod). `mobile-screen.tsx`/`mobile-screen-content.tsx` أعيد بناؤهما
  لتفعيل أزرار SMS وCALL الحقيقية (كانتا معطّلتين/وهميتين)، مع شاشة تغطية أثناء المكالمة (mute،
  hangup، عداد مدة) تُطابق أسلوب `call-overlay.tsx` الموجود لـin-app calling.
- **ملاحظة تصميم لـ`react-hooks/set-state-in-effect`** (قاعدة eslint صارمة موجودة مسبقًا في هذا
  المشروع): يُستخدم نمط `void Promise.resolve().then(load)` بدل `void load()` مباشرة داخل
  `useEffect` لتحميل البيانات الأولي (يطابق `use-contacts-screen.ts` الموجود)، وعداد مدة المكالمة
  يُصفَّر من داخل `handleCall` (حدث مستخدم فعلي) بدل تصفيره تفاعليًا داخل effect.
- `npm run typecheck:mobile` و`npm run lint:mobile` كلاهما نظيفان (التحذير الوحيد المتبقي
  `screenWidth` غير مستخدم في `animated-tab-bar.tsx` — سابق لهذا التحديث وغير متعلق به).
- **التالي مباشرة**: تحقّق محلي عبر `npx expo prebuild --clean` (لا يحتاج EAS، محلي بالكامل
  ومُتجاهَل من git) للتأكد فعليًا أن autolinking لا يُنشئ CocoaPods pod مكرر لنفس الحزمة (احتياط
  إضافي بعد حل تعارض الـnative module JS-level) — **قيد التنفيذ وقت كتابة هذا السطر**. بعده مباشرة:
  EAS dev-client build فعلي (طلب المستخدم صراحة تنفيذه بعد اكتمال كل شيء).
- **لم يُنفَّذ بعد عمدًا**: لوحة الأدمن، وأهم شيء: **اختبار حي فعلي مقابل حساب Telnyx حقيقي** — لا
  توجد بيانات اعتماد Telnyx أو Stripe في بيئة staging إطلاقًا حتى الآن (تأكدنا عبر
  `ssh g000st-web`). القائمة الكاملة لما هو مطلوب من المستخدم محفوظة في الذاكرة
  (`g000st-telephony-billing-keys-needed` في نظام الذاكرة الخاص بالجلسات).

## الأولوية التشغيلية التالية

**محدَّث 20 سبتمبر، نهاية اليوم — Chat/Social/ID/Contacts مغلقة تقنيًا ومنشورة:**

1. QA يدوي كامل بحسابين حقيقيين على staging لكل من Chat وSocial (نص/صورة/فيديو، لايك، تعليق،
   Camp، تقرير) وID/Profile (صورة، حفظ حقول، sign-out) وContacts (إضافة بـPublic ID، حذف،
   Online الحقيقي عبر heartbeat، فتح Chat من الصف) — لم يُنفَّذ رسميًا بعد لأي منها، رغم أن كل
   الفحوصات الآلية (typecheck/build/tests) ناجحة والنشر تم.
2. أنهِ/قرّر مصير عمل Tab Bar المتحرك غير المحفوظ سابقًا (`animated-tab-bar.tsx` +
   `(tabs)/_layout.tsx`) إن كان لا يزال معلَّقًا.
3. **التالي فعليًا**: تاب **Mobile** (اتصال، شحن رصيد، SMS) — القسم #6 في الخارطة أدناه. قبل أي
   كود: يحتاج قرار مزوّد اتصال حقيقي (Telnyx/Twilio أو غيره) **من المستخدم صراحة** قبل التنفيذ،
   ومراجعة سياسة Apple/Google In-App Purchase قبل بيع رصيد/خدمات رقمية داخل تطبيق iOS/Android.
   لا مفاتيح مزوّد تُوضع في Expo أو الويب أبدًا؛ الرصيد والاستهلاك يُحسبان في الباك فقط بـledger.
4. ميزة **Favorites في Contacts مستبعدة نهائيًا حاليًا بقرار صريح من المستخدم** — لا تُبنى ولا
   تُترك كتاب معطَّل في الواجهة؛ فقط All وOnline.

## نص جاهز لبدء جلسة جديدة

```text
اعمل على المشروع في /Volumes/wold/projects/g000st.
اقرأ AGENTS.md ثم docs/PROJECT_STATUS_AR.md والوثائق التي يشير إليها قبل أي تعديل.
ابدأ من قسم "الأولوية التشغيلية التالية"، ثم تابع حسب "خارطة التنفيذ المستمرة".
حافظ على الإنتاج دون تغيير ولا تسجل أي أسرار في Git.
```
